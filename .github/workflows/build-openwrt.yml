name: Build ImmortalWrt 25.12 Test for WR30U

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: wr30u-25.12-test
  cancel-in-progress: false

env:
  REPO_URL: https://github.com/chasey-dev/immortalwrt-mt798x-rebase
  REPO_BRANCH: "25.12"
  TZ: Asia/Shanghai

jobs:
  resolve-source:
    runs-on: ubuntu-22.04
    outputs:
      source_commit: ${{ steps.resolve.outputs.source_commit }}
    steps:
      - name: Checkout test configuration
        uses: actions/checkout@v4

      - name: Resolve 25.12 source revision
        id: resolve
        shell: bash
        run: |
          set -euo pipefail
          source_commit="$(git ls-remote "$REPO_URL" "refs/heads/$REPO_BRANCH" | awk '{print $1}')"
          test -n "$source_commit"
          echo "source_commit=$source_commit" >> "$GITHUB_OUTPUT"
          echo "25.12 source: $source_commit"

  build-test:
    needs: resolve-source
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Checkout test configuration
        uses: actions/checkout@v4

      - name: Install build dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
            bzip2 ccache clang cmake cpio curl device-tree-compiler ecj fastjar flex gawk \
            gettext gcc-multilib g++-multilib git gnutls-dev gperf haveged help2man intltool \
            lib32gcc-s1 libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev \
            libmpc-dev libmpfr-dev libncurses-dev libpython3-dev libreadline-dev libssl-dev \
            libtool libyaml-dev libz-dev lld llvm lrzsz mkisofs msmtp ninja-build p7zip-full \
            patch pkgconf python3 python3-docutils python3-pip python3-ply python3-pyelftools \
            qemu-utils re2c rsync scons squashfs-tools subversion swig texinfo uglifyjs \
            unzip vim wget xmlto xxd zlib1g-dev zstd
          sudo timedatectl set-timezone "$TZ"

      - name: Reclaim runner disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          df -hT

      - name: Clone pinned 25.12 source
        env:
          SOURCE_COMMIT: ${{ needs.resolve-source.outputs.source_commit }}
        run: |
          git init openwrt
          git -C openwrt remote add origin "$REPO_URL"
          git -C openwrt fetch --depth=1 origin "$SOURCE_COMMIT"
          git -C openwrt checkout --detach FETCH_HEAD
          test "$(git -C openwrt rev-parse HEAD)" = "$SOURCE_COMMIT"

      - name: Apply source customization
        run: bash diy-part1.sh "$GITHUB_WORKSPACE/openwrt"

      - name: Update and install feeds
        working-directory: openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Generate and validate 25.12 configuration
        run: |
          cd openwrt
          perl scripts/kconfig.pl + \
            defconfig/mt7981-ax3000.config \
            "$GITHUB_WORKSPACE/config/custom.config" > .config
          bash "$GITHUB_WORKSPACE/diy-part2.sh" "$PWD"
          sha256sum .config
          grep -qx 'CONFIG_USE_APK=y' .config
          grep -qx 'CONFIG_PACKAGE_apk-openssl=y' .config
          if ! cmp -s .config "$GITHUB_WORKSPACE/.config"; then
            echo "::warning::Generated .config differs from the committed 25.12 snapshot."
          fi

      - name: Verify WR30U 112M NMBM image contract
        working-directory: openwrt
        shell: bash
        run: |
          set -euo pipefail
          image_makefile=target/linux/mediatek/image/filogic.mk
          dts=target/linux/mediatek/dts/mt7981b-xiaomi-mi-router-wr30u-112m-nmbm.dts
          grep -A20 '^define Device/xiaomi_mi-router-wr30u-112m-nmbm$' "$image_makefile" > /tmp/wr30u-image.txt
          grep -q 'IMAGE/sysupgrade.bin := sysupgrade-tar | append-metadata' /tmp/wr30u-image.txt
          if grep -qE 'sysupgrade\.itb|KERNEL_IN_UBI|UBOOTENV_IN_UBI' /tmp/wr30u-image.txt; then
            echo "::error::Unexpected FIT/OpenWrt U-Boot image contract in NMBM target."
            exit 1
          fi
          grep -q 'reg = <0x600000 0x7000000>;' "$dts"
          grep -q 'mediatek,nmbm;' "$dts"
          grep -q 'mediatek,bmt-max-reserved-blocks = <64>;' "$dts"
          grep -q 'xiaomi,mi-router-wr30u-112m-nmbm' package/boot/uboot-tools/uboot-envtools/files/mediatek_filogic
          grep -q 'xiaomi,mi-router-wr30u-112m-nmbm' target/linux/mediatek/filogic/base-files/etc/board.d/01_leds
          test "$(grep -c 'xiaomi,mi-router-wr30u-112m-nmbm' target/linux/mediatek/filogic/base-files/etc/board.d/02_network)" -eq 2

      - name: Download sources
        working-directory: openwrt
        run: |
          make download -j"$(nproc)"
          find dl -type f -size -1024c -print -delete

      - name: Compile test firmware
        working-directory: openwrt
        run: make -j"$(nproc)" || make -j1 V=s

      - name: Collect test release firmware
        id: collect
        shell: bash
        env:
          SOURCE_COMMIT: ${{ needs.resolve-source.outputs.source_commit }}
        run: |
          set -euo pipefail
          output_dir="$GITHUB_WORKSPACE/openwrt/bin/targets/mediatek/filogic"
          release_dir="$GITHUB_WORKSPACE/release"
          device_prefix="immortalwrt-mediatek-filogic-xiaomi_mi-router-wr30u-112m-nmbm"
          test -d "$output_dir"
          mkdir -p "$release_dir"
          find "$output_dir" -maxdepth 1 -type f \
            -name "${device_prefix}*sysupgrade.bin" \
            -exec cp -t "$release_dir" -- {} +
          image_count="$(find "$release_dir" -maxdepth 1 -type f -name '*sysupgrade.bin' | wc -l)"
          if [[ "$image_count" -ne 1 ]]; then
            echo "::error::Expected exactly one WR30U 25.12 NMBM sysupgrade.bin, found $image_count."
            exit 1
          fi
          (cd "$release_dir" && sha256sum *sysupgrade.bin > sha256sums)
          source_short="${SOURCE_COMMIT:0:10}"
          release_tag="25.12-test-${source_short}-wr30u-nmbm-${GITHUB_RUN_NUMBER}"
          echo "release_dir=$release_dir" >> "$GITHUB_OUTPUT"
          echo "release_tag=$release_tag" >> "$GITHUB_OUTPUT"

      - name: Publish Chinese prerelease
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.collect.outputs.release_tag }}
          target_commitish: ${{ github.sha }}
          name: ImmortalWrt 25.12 WR30U 测试固件 ${{ steps.collect.outputs.release_tag }}
          prerelease: true
          body: |
            ## 测试固件信息

            - 状态：**预发布测试版，不替代现有 24.10 稳定固件**
            - 上游源码：`${{ env.REPO_URL }}`
            - 源码分支：`${{ env.REPO_BRANCH }}`
            - 源码提交：`${{ needs.resolve-source.outputs.source_commit }}`
            - 适用设备：小米 WR30U
            - 分区布局：112M UBI，NMBM（适配 hanwckf `immortalwrt-112m`）
            - 固件格式：`sysupgrade.bin`（传统 kernel/rootfs UBI 卷）
            - 默认管理地址：`192.168.2.1`
            - 内置组件：HomeProxy、drill、mtr-nojson、iperf3
            - UPnP：未编译

            ## 重要警告

            本固件恢复了上游已经移除的 WR30U 112M NMBM 第三方布局，用于兼容 hanwckf 多布局 U-Boot 的 `immortalwrt-112m` 模式。尚未完成 25.12 真机启动、保留配置升级和回退验证前，**不要在日常使用的路由器上直接保留配置升级**。测试前必须确认 U-Boot 恢复入口可用，并单独备份现有配置。

            Release 只包含一份 WR30U `sysupgrade.bin` 和 `sha256sums`；不提供 Actions artifact。
          files: ${{ steps.collect.outputs.release_dir }}/*
          fail_on_unmatched_files: true

      - name: Record successfully built 25.12 revision
        if: success()
        env:
          SOURCE_COMMIT: ${{ needs.resolve-source.outputs.source_commit }}
        run: |
          set -euo pipefail
          git pull --ff-only origin "$GITHUB_REF_NAME"
          printf '%s\n' "$SOURCE_COMMIT" > config/upstream-25.12.commit
          if git diff --quiet -- config/upstream-25.12.commit; then
            echo "25.12 source revision is already recorded."
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add config/upstream-25.12.commit
          git commit -m "Record built 25.12 upstream ${SOURCE_COMMIT:0:10}"
          git push origin "HEAD:$GITHUB_REF_NAME"
